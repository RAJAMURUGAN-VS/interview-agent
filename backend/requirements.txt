flask
flask-cors
flask-sqlalchemy
python-dotenv
# Auth dependencies
google-auth
PyJWT
langchain
langgraph
langchain-google-genai
langchain-perplexity
assemblyai
requests
langchain-community
langchain-huggingface
langchain-chroma
chromadb
sentence-transformers
firecrawl-py
youtube-transcript-api
numpy<2.0
tifffile<2025.0

# PDF extraction — fast C-backed engine (replaces pypdf/PyPDFLoader)
pymupdf

# OCR for scanned / image-based PDF pages (Tesseract + Ghostscript required)
# System deps: apt-get install -y tesseract-ocr ghostscript  (or choco install on Windows)
ocrmypdf

# Legacy OCR packages — no longer used by rag_service after OCRmyPDF migration.
# Remove these once confirmed nothing else in the codebase imports them.
paddlepaddle
paddleocr

# Image processing (required by PaddleOCR — keep until paddle packages removed)
pillow
opencv-python-headless